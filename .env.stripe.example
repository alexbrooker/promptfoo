# Stripe Backend Configuration for Promptfoo
# Copy these variables to your .env file (create .env in project root if it doesn't exist)

# Stripe Secret Key (Server-side)
STRIPE_SECRET_KEY=sk_test_...

# Stripe Webhook Secret (for webhook endpoint security)
STRIPE_WEBHOOK_SECRET=whsec_...

# Stripe Price IDs (created in Stripe Dashboard for each subscription plan)
STRIPE_BASIC_PRICE_ID=price_1234567890
STRIPE_PRO_PRICE_ID=price_1234567891  
STRIPE_ENTERPRISE_PRICE_ID=price_1234567892

# Optional: Database URL (if not using default SQLite)
# DATABASE_URL=sqlite:./promptfoo.db

# Optional: Self-hosted flag
# PROMPTFOO_SELF_HOSTED=true